/*import { remove, eventValidator } from "../../models/eventModel.js"

export default async function deleteEventController(req ,res, next) {
    try {
        const {id} = req.params
        const user = {
            id: +id
        }
        const {sucess, error, data} = eventValidator ()
    }
}
    */